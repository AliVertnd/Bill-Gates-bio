# Статус полного добора данных (v1.0)

## Сделано
- Росстат/BD PMO: население, миграция, ЗП/доходы, цены, ввод, КСР → `working/04` + `data/*.csv`
- Конкуренты + DOM ID → `working/05`
- Gateway rental model → `working/06`
- IMPRO контуры (драфт) → `working/07`
- Финальная выжимка для маркетинг-директора → `deliverables/00_…`

## Не закрыто / блокеры
- `Impro.Group_Documents` / ветка `cursor/research-agency-docs-framework-5433` — **этот агент не видит**
  - GitHub API → 404 (private, токен чата привязан к `Bill-Gates-bio`)
  - Cloud agents list → только агенты **этого** репо (1 шт.). Чужой чат/ветка IMPRO недоступны отсюда
  - Environment у рана = null (нет multi-repo environment)
  - «Полный доступ к репозиториям» в другом чате / API Cursor **не переносится** автоматически в этот ран
- Помесячные ДДУ Росреестр по РА — публичного ряда нет (есть косвенно: ERZ/Selfstroit/Morein)
- наш.дом.рф UI / Fedstat — 403 из среды (частично обошли индексами/XLSX)
- Achieved occupancy посутки ГА — только модель-сценарии
- Внутренние CRM/бюджет СД — нет

## Как снять блокер IMPRO docs (любой 1 способ)
1. В чате на `Impro.Group_Documents` (ветка `…-5433`): попросить агента **выгрузить ключевые файлы модели/прайса** текстом или zip в ответ → переслать сюда  
2. Сделать репо доступным integration’у этого рана / добавить collaborator + перезапустить агента **на том репо**  
3. Вручную скопировать в этот workspace папку `docs/` из IMPRO (paste / upload)  
4. Создать Cloud Environment с **обоими** репо и стартовать агента оттуда

## Следующий апгрейд при доступе
1) IMPRO docs → финальный прайс пакетов  
2) Выгрузка ЕИСЖС/Росреестр из кабинета клиента  
3) AirDNA/кабинет Суточно по ГА  
4) Медиаскоп/кабинеты Ads для CPL бенчмарков
